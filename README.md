## Scuba Hub Adventures – Local Setup

This project is a React (Vite) front‑end for the **Scuba Hub Adventures** website.

### Run Locally

**Prerequisite:** Node.js

1. Install dependencies:  
   `npm install`
2. Start the development server:  
   `npm run dev`
